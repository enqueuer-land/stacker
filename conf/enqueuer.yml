requisitions:
  run-mode:
    daemon:
        -
            type: standard-input
outputs:
    -
        type: standard-output
    # - 
    #     type: file
    #     filenamePrefix: results

log-level: warn
requisitions:
  run-mode:
    daemon:
        -
            type: uds
            path: enqueuer-message-sender
outputs:
    -
        type: uds
        path: enqueuer-message-receiver

log-level: warn